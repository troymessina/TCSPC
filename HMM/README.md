* Intensity
  * Code and an example for analyzing intensity-based photobleaching. The code determines the number of constituent fluorescent dyes that exists base on photobleaching steps. The steps are identified by Bayes Information Criterion comparison from the log-likelihood returned from a Markov chain analysis.
